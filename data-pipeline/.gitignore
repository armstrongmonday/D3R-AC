__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Local state store / audit log (regenerated at runtime, environment-specific)
*.sqlite3
*.log

# Regenerated every cycle by scripts/run_cycle.py — the copy that matters
# is frontend/public/data/communities.json, which IS committed since it's
# what the site actually serves as its default/demo feed.
output/
